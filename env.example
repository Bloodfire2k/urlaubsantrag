# Environment Variables für die Anwendung
# Kopiere diese Datei zu .env und setze deine eigenen Werte

# JWT Secret (MUSS gesetzt werden - generiere einen sicheren Zufallswert)
JWT_SECRET=your-super-secure-jwt-secret-here

# Datenbank-Konfiguration
DB_TYPE=postgres
DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# Server-Konfiguration
PORT=3001
NODE_ENV=development

# JSON-DB Bootstrap (nur auf dem Server setzen - Coolify Runtime):
JSON_DB_RESET_ON_DEPLOY=1
JSON_DB_FORCE_RESET=0

# CORS-Konfiguration
ALLOWED_ORIGIN=http://localhost:3000

ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
ADMIN_EMAIL=admin@example.com